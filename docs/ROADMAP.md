# fivewhys 路线图

> 上游文档：[REQUIREMENTS.md](REQUIREMENTS.md)（要什么、为什么）。
> 本文档回答「分几步做、每步做到什么算完成」。

九个里程碑。**每个都有验收标准，达不到就不进入下一个。**

三条拆解原则：

1. **先竖切，再横扩** —— 第一步就做「一个服务 + 一种故障 + 一个工具」的完整链路，
   哪怕很糙。先证明路走得通，再变复杂。
2. **尽早拿到第一个数字** —— M6 必须产出真实评测数据。如果项目不行，
   你在第 3 周就发现了，而不是第 4 周。
3. **每个里程碑结束都有能跑的东西** —— 不允许出现「写了三天但什么都跑不了」的阶段。

---

## ✅ M0 · 脚手架 · 0.5 天

- [x] 仓库、目录结构、`pyproject.toml`、依赖清单
- [x] 核心数据模型 `models.py`
- [x] 工具注册表 `ToolRegistry`
- [x] mock 日志存储与服务
- [x] `fivewhys doctor` 可用
- [x] 冒烟测试通过

**验收**：`pytest` 通过，`fivewhys doctor` 全绿。← 已完成

---

## 🟡 M1 · 端到端最细竖切 · 3 天 【当前】

**目标**：一个假服务、一种故障、一个工具，agent 能诊断出来。

对应需求：FR-1（1 个服务）、FR-2（1 种故障）、FR-3、FR-4、FR-5（`query_logs`）、FR-6

- [x] **M1-2** 实现 `mock/scenarios.py::inject_db_pool_exhausted` ✅ 已完成（FIV-1）
- [ ] **M1-3** 实现 `tools/query_logs.py::_query` ⬜ FIV-2
- [ ] **M1-4** 实现 `agent/loop.py::diagnose` ⬜ FIV-4
- [ ] **M1-5** 手工跑一次：注入故障 → 问 agent → 看它能否查日志得出正确结论 ⬜ FIV-5

**验收**：跑 5 次，至少 3 次给出正确根因。

> ⚠️ 这一步故意做糙。不要美化、不要抽象、不要想着「设计一个通用框架」。
> 目的只有一个：**验证这条路走得通**。丑是正常的。

---

## ⬜ M2 · 把模拟系统做真 · 3.5 天

- [ ] M2-1 扩展到 3 个服务（order / payment / inventory），加上调用依赖关系
- [ ] M2-2 让服务产生指标（QPS、错误率、P95 延迟）
- [ ] M2-3 加配置文件和发布历史
- [ ] M2-4 定义 `ground_truth.yml` 的落盘格式

**验收**：3 个服务互相调用，日志 / 指标 / 配置三类数据都能查。

---

## ⬜ M3 · 故障库 · 2.5 天

- [ ] M3-1 把注入逻辑抽成统一接口（现在才做抽象，不早不晚）
- [ ] M3-2 实现 **5 种**故障
- [ ] M3-3 每种故障标注 ground truth
- [ ] M3-4 写快照脚本：注入后导出全部数据，保证可复现

**验收**：`python -m fivewhys.inject <fault_name>` 5 种都能注入，且
**只在日志 / 指标里留下「现象」，不直接暴露答案**。

> 💡 M3-4 的「不暴露答案」很重要。如果日志里直接写着
> `ERROR: database pool exhausted`，项目就没难度了，面试官也会质疑。

---

## ⬜ M4 · 工具层补齐 · 2 天

- [ ] M4-1 补齐 5 个工具：查日志 / 查指标 / 查配置 / 查发布 / 查依赖
- [ ] M4-2 精心写工具描述（这是 prompt 工程的核心）
- [ ] M4-3 每个工具写单测

**验收**：每个工具有单测；工具描述能说清「什么场景该用它」。

---

## ⬜ M5 · Agent 循环加固 · 2.5 天

- [ ] M5-1 接入 litellm（多模型可切换）
- [ ] M5-2 Pydantic 结构化输出约束 + 校验证据来源真实性
- [ ] M5-3 终止条件：5 Whys 深度 / max_steps / max_cost
- [ ] M5-4 **trace 落盘**：每步的 prompt / 响应 / 工具结果 / token 数

**验收**：能跑完 5 个场景，每次都输出结构化诊断结论。

> M5-4 别跳过。这些 trace 是后面分析失败、讲取舍的唯一依据。

---

## ⬜ M6 · 评测台 · 2.5 天 【关键】

- [ ] M6-1 评测执行器：N 个场景 × M 次
- [ ] M6-2 指标计算：准确率 / 成本 / 延迟 / 工具调用次数
- [ ] M6-3 报告输出成 markdown 表格
- [ ] M6-4 **缓存 LLM 响应**（省钱 + 可复现）

**验收**：一条命令跑出完整的 baseline 数字表。

> 🎯 这里是第一个真实产出。拿到这张表，就能判断项目值不值得继续。

---

## ⬜ M7 · 改进迭代 · 5.5 天 【项目价值全在这】

一周就干一件事：**改一次，跑一次全量，记录一行**。

- [ ] M7-1 加 Pydantic 约束 → 跑全量 → 记录
- [ ] M7-2 加失败重试 → 跑全量 → 记录
- [ ] M7-3 **调工具粒度** → 跑全量 → 记录（提升最大，最有讲头）
- [ ] M7-4 加步数 / 成本熔断 → 跑全量 → 记录

**验收**：有一张 4 行的改进曲线表。

> 🎯 产出就是简历上那行「从 X% 提升到 Y%」。

---

## ⬜ M8 · 扩展与包装 · 3.5 天

- [ ] M8-1 故障补到 20 种
- [ ] M8-2 Docker 化
- [ ] M8-3 README：数字表 + 架构图 + **取舍分析**
- [ ] M8-4 失败模式分析文档

**验收**：**陌生人克隆仓库后 5 分钟内能跑出结果。**

---

## 总览

| 里程碑 | 天数 | 能不能砍 |
| ------ | ---- | -------- |
| M0 脚手架 | 0.5 | 不能 ✅ |
| M1 竖切 | 3 | 不能 ← 当前 |
| M2 系统做真 | 3.5 | 不能 |
| M3 故障库 | 2.5 | 不能 |
| M4 工具层 | 2 | 不能 |
| M5 循环加固 | 2.5 | 不能 |
| M6 评测台 | 2.5 | 不能 |
| M7 改进迭代 | 5.5 | 不能（项目价值全在这） |
| M8 包装 | 3.5 | M8-4 可砍，M8-1 可减到 10 种 |
| **合计** | **≈25.5 天** | |

## 两个提醒

**时间不够怎么办**：M1–M6 + M7 做一半，**已经是一个能写进简历的完整项目**。
20 种故障、Docker、blog 都是锦上添花，别为了它们牺牲 M7。

**判断点**：如果 M6 跑出来的 baseline 准确率高于 90%，说明故障设计太简单了，
回 M3 把「表象」做得更模糊。**baseline 越低越好**，因为那意味着更大的提升空间可讲。
